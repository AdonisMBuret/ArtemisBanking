@model ArtemisBanking.Application.ViewModels.Cajero.RetiroCajeroViewModel
@{
    ViewData["Title"] = "Realizar Retiro";
}

<div class="container-artemis fade-in">
    <div class="row justify-content-center">
        <div class="col-md-8 col-lg-6">
            <div class="card-artemis">
                <div class="card-header-artemis" style="background: var(--morado-oscuro);">
                    <h3 class="mb-0">
                        <i class="bi bi-dash-circle-fill me-2"></i>Realizar Retiro
                    </h3>
                </div>
                <div class="card-body">
                    <p class="text-muted">Ingrese los datos de la cuenta de la que desea retirar fondos.</p>

                    <!-- Formulario de Retiro -->
                    <form asp-action="Retiro" method="post">
                        @Html.AntiForgeryToken()

                        <!-- Mensaje de error general -->
                        <div asp-validation-summary="ModelOnly" class="alert alert-artemis-error" role="alert"></div>

                        <!-- Campo: Número de Cuenta Origen -->
                        <div class="mb-3">
                            <label asp-for="NumeroCuentaOrigen" class="form-label-artemis"></label>
                            <input asp-for="NumeroCuentaOrigen" class="form-control form-control-artemis" placeholder="Ingrese los 9 dígitos de la cuenta" />
                            <span asp-validation-for="NumeroCuentaOrigen" class="text-danger"></span>
                        </div>

                        <!-- Campo: Monto a Retirar -->
                        <div class="mb-3">
                            <label asp-for="Monto" class="form-label-artemis"></label>
                            <input asp-for="Monto" class="form-control form-control-artemis" type="number" step="0.01" placeholder="Ej: 500.00" />
                            <span asp-validation-for="Monto" class="text-danger"></span>
                        </div>

                        <!-- Botones de Acción -->
                        <div class="d-flex justify-content-between gap-2 mt-4">
                            <a asp-action="Index" class="btn btn-outline-artemis">
                                <i class="bi bi-x-circle me-2"></i>Cancelar
                            </a>
                            <button type="submit" class="btn btn-artemis-secondary">
                                <i class="bi bi-check-circle-fill me-2"></i>Procesar Retiro
                            </button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}