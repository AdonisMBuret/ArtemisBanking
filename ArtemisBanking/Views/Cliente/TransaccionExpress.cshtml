@model ArtemisBanking.Application.ViewModels.Cliente.TransaccionExpressViewModel
@{
    ViewData["Title"] = "Transacción Express";
}

<div class="container-artemis fade-in">
    <div class="row justify-content-center">
        <div class="col-md-8 col-lg-6">
            <div class="card-artemis">
                <div class="card-header-artemis">
                    <h3 class="mb-0"><i class="bi bi-send-fill me-2"></i>@ViewData["Title"]</h3>
                </div>
                <div class="card-body">
                    <p class="text-muted">Transfiera a cualquier cuenta de Artemis Banking.</p>

                    <form asp-action="TransaccionExpress" method="post">
                        @Html.AntiForgeryToken()
                        <div asp-validation-summary="ModelOnly" class="alert alert-artemis-error" role="alert"></div>

                        <div class="mb-3">
                            <label asp-for="CuentaOrigenId" class="form-label-artemis"></label>
                            <select asp-for="CuentaOrigenId" class="form-select form-control-artemis" asp-items="Model.CuentasDisponibles">
                                <option value="">Seleccione una cuenta...</option>
                            </select>
                            <span asp-validation-for="CuentaOrigenId" class="text-danger"></span>
                        </div>

                        <div class="mb-3">
                            <label asp-for="NumeroCuentaDestino" class="form-label-artemis"></label>
                            <input asp-for="NumeroCuentaDestino" class="form-control form-control-artemis" placeholder="9 dígitos de la cuenta destino" />
                            <span asp-validation-for="NumeroCuentaDestino" class="text-danger"></span>
                        </div>

                        <div class="mb-3">
                            <label asp-for="Monto" class="form-label-artemis"></label>
                            <input asp-for="Monto" class="form-control form-control-artemis" type="number" step="0.01" placeholder="Ej: 500.00" />
                            <span asp-validation-for="Monto" class="text-danger"></span>
                        </div>

                        <div class="d-flex justify-content-between gap-2 mt-4">
                            <a asp-action="Index" class="btn btn-outline-artemis">
                                <i class="bi bi-x-circle me-2"></i>Cancelar
                            </a>
                            <button type="submit" class="btn btn-artemis-primary">
                                Siguiente<i class="bi bi-arrow-right-circle ms-2"></i>
                            </button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}